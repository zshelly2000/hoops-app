// Cookie naming the chosen universe when a user belongs to more than one.
// Standalone module (no next/headers import) so both middleware and
// server code can share it.
export const UNIVERSE_COOKIE = 'hoops-universe'
